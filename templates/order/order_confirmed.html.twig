{% extends 'base.html.twig' %} {% block title %}Commande confirmée{% endblock %}
{% block body %}
<div class="container mx-auto px-4 py-8 text-center">
  <h1 class="text-3xl font-bold text-green-600 mb-4">Commande confirmée !</h1>
  <p>Merci pour votre commande.</p>
  <p>
    Adresse de livraison : <strong>{{ order.deliveryAddress }}</strong>
  </p>

  <div class="mt-6">
    <a href="{{ path('app_home') }}" class="text-blue-600 hover:underline">
      ⬅ Retour à l'accueil
    </a>
  </div>
</div>
{% endblock %}
